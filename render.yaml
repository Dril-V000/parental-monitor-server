services:
  - type: web
    name: parental-monitor-server
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: API_SECRET
        sync: false
      - key: DISCORD_WEBHOOK_URL
        sync: false
 
